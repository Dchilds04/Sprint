<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Achievements - MoneyMogul</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #2d4a2d;
      color: #d4e157;
      padding: 20px;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background: #3b573b;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(212, 225, 87, 0.5);
    }
    .achievement {
      background-color: gold;
      padding: 10px;
      margin: 10px 0;
      border-radius: 5px;
      color: #000;
      font-weight: bold;
      display: flex;
      align-items: center;
    }
    .badge {
      width: 30px;
      height: 30px;
      margin-right: 10px;
    }
    nav a {
      color: #d4e157;
      margin-right: 15px;
      text-decoration: none;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <nav>
    <a href="dashboard.html">Dashboard</a>
    <a href="goals.html">Goals</a>
    <a href="achievements.html">Achievements</a>
    <a href="index.html" onclick="logout()">Logout</a>
  </nav>

  <div class="container">
    <h1>Achievements</h1>
    <div id="achievementList"></div>
  </div>

  <script>
    if (!localStorage.getItem('currentUser')) {
      window.location.href = 'index.html';
    }

    function logout() {
      localStorage.removeItem('currentUser');
    }

    const goals = JSON.parse(localStorage.getItem('goals')) || [];
    const finances = JSON.parse(localStorage.getItem('finances')) || {};
    const achievements = [];
    const completeGoals = goals.filter(g => g.progress >= 100);

    // Goal-based achievements
    if (completeGoals.length >= 1) achievements.push({ text: "🏆 First Goal Achieved!", badge: "https://img.icons8.com/emoji/48/trophy-emoji.png" });
    if (completeGoals.length >= 3) achievements.push({ text: "🎯 Triple Goal Master!", badge: "https://img.icons8.com/emoji/48/check-mark-emoji.png" });
    if (goals.length >= 5) achievements.push({ text: "📘 Long-Term Visionary", badge: "https://img.icons8.com/emoji/48/star-emoji.png" });

    // Finance-based achievements
    if (finances.totalSaved >= 500) achievements.push({ text: "💵 Saved $500!", badge: "https://img.icons8.com/color/48/money.png" });
    if (finances.totalSaved >= 1000) achievements.push({ text: "💰 Saved $1,000!", badge: "https://img.icons8.com/color/48/banknotes.png" });
    if (finances.totalSaved >= 5000) achievements.push({ text: "🏦 Saved $5,000!", badge: "https://img.icons8.com/color/48/safe.png" });

    const list = document.getElementById('achievementList');
    if (achievements.length === 0) {
      list.innerHTML = '<p>No achievements yet. Keep working toward your goals!</p>';
    } else {
      achievements.forEach(a => {
        list.innerHTML += `<div class='achievement'><img class='badge' src='${a.badge}' alt='badge'>${a.text}</div>`;
      });
    }
  </script>
</body>
</html>
